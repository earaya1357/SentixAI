{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "Base",
            "database": "${workspaceFolder:SentimentGemini}/db/Base.db"
        }
    ]
}